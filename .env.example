# Old PHP Database (read-only)
OLD_DB_HOST=localhost
OLD_DB_PORT=5432
OLD_DB_NAME=marketplace
OLD_DB_USER=loanbud
OLD_DB_PASSWORD=loanbud
OLD_DB_SSL=false

# New Kotlin Database (read-write)
NEW_DB_HOST=localhost
NEW_DB_PORT=5432
NEW_DB_NAME=local
NEW_DB_USER=local
NEW_DB_PASSWORD=local

# SSH Tunnel (optional — for connecting to remote Old DB via bastion host)
# SSH_KEY_PATH=../dev-mgmt-ec2
# SSH_BASTION_HOST=x.x.x.x
# SSH_BASTION_USER=ec2-user
# SSH_REMOTE_DB_HOST=your-rds-endpoint.rds.amazonaws.com
# SSH_REMOTE_DB_PORT=5432
# SSH_AUTO_TUNNEL=true

# S3 URL Rewriting (optional — rewrites profile_photo_path URLs from old bucket to new)
# S3_OLD_BUCKET_URL=https://brokertool-dev-private-bucket.s3.us-east-1.amazonaws.com
# S3_NEW_BUCKET_URL=https://loanbud-dev-bucket.s3.us-east-1.amazonaws.com

# Migration settings
BATCH_SIZE=500
DRY_RUN=false
